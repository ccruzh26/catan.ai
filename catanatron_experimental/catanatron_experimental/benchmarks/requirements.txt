# Minimal dependencies for catanatron_core + alpha-beta or MCTS

Flask==2.2.3

# catanatron_core dependencies
networkx==2.8.8
click==8.1.3
numpy==1.21.6
python-dateutil==2.8.2
requests==2.28.2

# If you plan to use MCTS from catanatron_experimental, you might need:
# (Double-check catanatron_experimental for any extra libs needed; but usually the above is enough)

# If for some reason there's a library you see in the code that isn't listed, add it:
# e.g., "pydash==5.1.0" or "colorama==0.4.6" if catanatron references them.
